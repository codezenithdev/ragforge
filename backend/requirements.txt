# --- Web framework / server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
httpx==0.28.1
slowapi==0.1.9                    # per-identity rate limiting (Redis-backed)
gunicorn==23.0.0                  # prod process manager (uvicorn workers)

# --- Relational DB (documents, briefs, sub-queries) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# --- Vector store (ChromaDB client; pinned to match the
#     chromadb/chroma:1.5.9 server image in docker-compose.yml) ---
chromadb==1.5.9

# --- Config ---
pydantic==2.10.4
pydantic-settings==2.7.1          # BaseSettings moved here in Pydantic v2

# --- LLMs / orchestration ---
anthropic==0.42.0
openai==1.59.2
instructor==1.7.2
langchain-core==0.3.28
langchain-anthropic==0.3.1
langchain-openai==0.2.14          # LangChain embeddings wrapper for ragas
langchain-community==0.3.13        # required by ragas 0.2.x (still ships chat_models.vertexai)
langgraph==0.2.60

# --- Retrieval / re-ranking / chunking ---
rank-bm25==0.2.2
sentence-transformers==3.3.1
nltk==3.9.1                       # sentence tokenizer for the semantic chunker

# --- Ingestion / loaders ---
pdfplumber==0.11.4
python-docx==1.1.2
beautifulsoup4==4.12.3            # WebLoader httpx + BeautifulSoup fallback
tavily-python==0.5.0

# --- Evaluation ---
ragas==0.2.10

# --- Job queue / cache ---
redis==5.2.1
celery==5.4.0

# --- Utilities ---
tenacity==9.0.0                   # exponential-backoff retry for the embedder

# --- Testing ---
pytest==9.0.3
pytest-asyncio==0.25.0
pytest-mock==3.14.0
respx==0.22.0                     # httpx request mocking

# --- Lint / type checking (CI) ---
ruff==0.8.6
mypy==1.14.1
