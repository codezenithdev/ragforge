server {
    listen 3000;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: client-side routes like /briefs/:id must serve index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
